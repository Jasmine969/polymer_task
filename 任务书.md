# 面试小作业：从体系模型中拆出单链并完成 GROMACS 动力学流程

## **参考论文**：Kangjie Lyu, Yanqiu Peng, Li Xiao, Juntao Lu, Lin Zhuang. *Water induced phase segregation in hydrocarbon proton exchange membranes*. **Journal of Energy Chemistry, 27 (2018) 1517–1520**.DOI： `10.1016/j.jechem.2018.05.009`

## 作业目标

本作业主要考察你是否真正理解以下事项：

1. 如何从一个现成体系模型中拆出目标单链，并整理成可用于模拟的输入。
2. 如何在 GROMACS 中组织多分子体系的 `top` / `itp` 文件。
3. 是否理解 `EM -> NVT -> NPT -> production` 的完整动力学流程，而不是只会照抄命令。
4. 是否能在结果不理想时，对问题进行基本排查和解释。请把重点放在 **体系拆分、拓扑组织、动力学流程和结果分析** 上。

## 任务总览

| 任务       | 内容                                                        | 权重 |
| :--------- | :---------------------------------------------------------- | :--- |
| **任务一** | 从给定体系模型中拆出目标单链，并整理单链输入                | 25%  |
| **任务二** | 排查并修正聚合物 `.itp` 中的错误条目                        | 20%  |
| **任务三** | 建立 `top` / `itp` 组织，完成单链在两种溶剂条件下的 MD 流程 | 40%  |
| **任务四** | 输出分析结果，并说明异常结果时的排查思路                    | 15%  |

## 已提供文件

| 文件               | 路径        | 说明                   |
| :----------------- | :---------- | :--------------------- |
| `CLS_10chains.gro` | `polymer/`  | 给定的体系模型坐标文件 |
| `CLS_10chains.itp` | `topology/` | 含错误的聚合物拓扑文件 |
| `water.itp`        | `solvent/`  | SPC/E 水模型           |
| `em.mdp`           | `mdp/`      | 能量最小化参数         |
| `nvt.mdp`          | `mdp/`      | NVT 平衡参数           |
| `npt.mdp`          | `mdp/`      | NPT 平衡参数           |
| `nvt_prod.mdp`     | `mdp/`      | 生产运行参数           |

说明：

- `CLS_10chains.gro` 不是可以直接拿来作为最终研究对象的"现成单链输入"。
- 你需要先从给定体系模型中识别并拆出目标单链，再围绕该单链继续后续工作。

## 任务一：拆分体系并得到单链输入

### 排查要求

请基于给定的 `polymer/CLS_10chains.gro` 完成以下工作：

1. 识别体系中哪些原子属于目标聚合物链。
2. 拆出一个可单独用于模拟的单链坐标文件。
3. 说明你使用的命令、脚本或处理逻辑。
4. 简要说明你如何确认拆出的结果是自洽的。

### 你需要展示的内容

- 原始体系模型中你观察到的分子组织方式
- 拆链后的坐标文件
- 拆链过程中的关键命令或脚本
- 你对拆分结果的检查方式

### 我们想看什么

- 你是否能从现有坐标文件中读懂体系组织方式
- 你是否能把"体系模型"转换成"目标单链输入"
- 你是否会检查输出，而不是盲目继续后续步骤

## 任务二：排查并修正聚合物拓扑错误

### 背景

GROMACS 的 `top` / `itp` 文件定义了分子中的拓扑关系。拓扑文件与坐标文件必须匹配，否则会在 `grompp` 阶段报错，或者导致后续模拟不可信。

### 要求

请使用以下文件完成排查：

- `topology/CLS_10chains.itp`
- 你在任务一中得到的单链坐标文件你需要：

1. 尝试生成 `.tpr`
2. 观察报错或警告
3. 定位出有问题的条目
4. 修正该问题
5. 再次生成 `.tpr`，确认问题已消除

### 提示

- 可以结合可视化工具(VMD, Avagadro)或对原子编号的检查来定位问题
  - 提供的文件需要包含键连关系，比如pdb或者mol2格式
- gromacs中结构的化学键的连接关系在itp文件中，gro本身没有键连关系
- 除了修正错误，我们还想看到你如何解释"为什么这里是错的"

## 任务三：组织 `top` / `itp` 并完成 MD 流程

### 目标

围绕你拆出的单链，构建两套体系并完成模拟：

- 良溶剂条件：纯水
- 劣溶剂条件：水 / 乙醇混合溶剂

### 3.1 溶剂拓扑

请完成：

1. 水模型直接使用 `solvent/water.itp`
2. 乙醇拓扑请自行生成，并整理成可以被主 `top` 文件包含的形式例如可以使用：

This content is only supported in a Feishu Docs

### 3.2 `top` / `itp` 组织

请自行整理一个清晰的文件组织方式，至少包含：

- 聚合物单链 `itp`
- 水分子 `itp`
- 乙醇分子 `itp`
- 主系统 `top`你需要明确展示：

1. 主 `top` 中包含了哪些 `itp`
2. `[ molecules ]` 段如何书写
3. 坐标文件中的分子数与拓扑中的分子数如何对应
4. 你的文件组织为什么适合多分子体系题面不要求你采用唯一标准答案，但要求你的组织方式清晰、可运行、可解释。

### 3.3 构建模拟体系

用 PackMol 或其他你熟悉的方式构建初始盒子。你至少需要给出两套体系：

1. 单链 + 水
2. 单链 + 水 / 乙醇混合溶剂比例可参考：

- 纯水：约 `1500` 个水分子
- 混合溶剂：约 `1050` 个水分子 + `450` 个乙醇分子你也可以在合理范围内调整数量，但需要说明理由。

### 3.4 动力学流程

请完成以下流程：

1. 能量最小化 `EM`
2. `NVT` 平衡
3. `NPT` 平衡
4. 生产运行 `production`除了给出命令，请补充说明：

- 每一步的输入文件和输出文件是什么
- 每一步的主要目的是什么
- 你如何判断这一阶段至少"基本正常"

## 任务四：轨迹分析与异常排查

### 必做分析

请至少计算并展示以下指标：

- 回转半径 `Rg(t)`
- 末端距 `Ree(t)`
- `Rg` 的简单收敛性检查你可以使用例如：
- `gmx gyrate`
- `gmx distance`
- `gmx analyze`

### 结果讨论

请比较两种溶剂条件下的差异，并回答：

1. 两种条件下 `Rg` 和 `Ree` 的趋势是否一致
2. 如果趋势不符合预期，你最先检查什么
3. 你认为当前结果最可能受哪一步影响

### 预期趋势

通常情况下，可预期：

| 指标    | 纯水 | 水 / 乙醇 | 趋势 |
| :------ | :--- | :-------- | :--- |
| `⟨Rg⟩`  | 较大 | 较小      | 下降 |
| `⟨Ree⟩` | 较大 | 较小      | 下降 |

## 如果你的结果与此不同，不一定影响评价；关键在于你是否能做出合理解释。

## 提交要求

请提交以下内容：

1. 一份简要报告
2. 你拆出的单链坐标文件
3. 修正后的聚合物 `itp`
4. 组织好的主 `top` 与相关 `itp`
5. 关键脚本、命令记录或工作流说明
6. 关键分析结果图或结果摘要

## 报告建议结构

建议至少包含以下部分：

### 1. 体系拆分

- 原始 `gro` 的理解
- 单链拆分过程
- 结果检查方式

### 2. 拓扑修正与组织

- 错误条目定位过程
- 修改内容
- `top` / `itp` 组织说明

### 3. 模拟流程

- 建盒方式
- `EM / NVT / NPT / production` 命令与说明
- 运行中遇到的问题

### 4. 结果分析

- `Rg`
- `Ree`
- 两组体系对比
- 异常结果时的排查思路

## 评估重点

我们重点看以下几点：

1. 是否能从体系模型中拆出目标单链
2. 是否理解 GROMACS 中 `top` / `itp` 的组织方式
3. 是否能定位并修复拓扑错误
4. 是否理解完整 MD 流程的每一步在做什么
5. 是否能对结果做出基本解释和排查

## 建议完成时间

建议投入时间：2-4 天。

## 配套文件结构

This content is only supported in a Feishu Docs

This content is only supported in a Feishu Docs